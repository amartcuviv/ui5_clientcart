# yaml-language-server: $schema=https://sap.github.io/ui5-tooling/schema/ui5.yaml.json

specVersion: "3.1"
metadata:
  name: fioriamlclient.fioritestclient
type: application
builder:
  resources:
    excludes:
      - /test/**
      - /localService/**
  customTasks:
    - name: deploy-to-abap
      afterTask: generateCachebusterInfo
      configuration:
        target:
          url: http://cuvawss4d01.linkeit.local:50000/
          client: '100'
        app:
          name: FIORICLIENTEST
          package: ZDMO_FIORI
          transport: S4DK919422
        exclude:
          - /test/
